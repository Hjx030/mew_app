# MewCode v0.3 — Agent Loop + Plan Mode Checklist

## 实现完整性
- [ ] [步数显示] Agent Loop 执行中 TUI 显示当前步数（如 "─── 步骤 2/10 ───"）
- [ ] [循环执行] 工具执行后再次带 tools 调 API，模型可连续调用多个工具
- [ ] [终止条件] 模型返回文字回复时循环结束
- [ ] [最大步数] 超过 10 步强制停止，显示提示
- [ ] [自动确认] 同一轮中首步确认后，后续同类工具自动放行
- [ ] [Ctrl+C 中断] 循环期间 Ctrl+C 终止并回到输入
- [ ] [Plan Mode 引导] 系统提示词引导 AI 对多步任务先出计划
- [ ] [/plan 命令] `/plan on` 和 `/plan off` 切换模式

## 端到端场景
- [ ] **场景 1（多步链式）**：输入"创建 hello.py，写入 Hello World，然后读取它" → write_file → read_file → 文字回复，显示步骤 1/10、2/10
- [ ] **场景 2（Plan Mode）**：输入一个复杂任务 → AI 输出计划 → 用户回复"开始" → AI 执行工具
- [ ] **场景 3（取消工具）**：bash 调用时输入 n → 取消执行，模型继续后续
- [ ] **场景 4（Ctrl+C 中断）**：循环执行中按 Ctrl+C → 中断
