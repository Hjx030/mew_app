# MewCode 安全检查层 Spec (v0.5)

## 背景

MewCode 已完成 Agent Loop（v0.3）和缓存感知的 Prompt 重构（v0.4），当前有 6 个工具（read/write/edit/glob/grep/bash）。**现有的安全机制只有一个 `needs_confirmation`**：只有 bash 工具会在执行前弹一次 `[Enter/n]` 确认。存在四个缺口：

1. **无主动拦截**：模型在确认前就可以发起 `rm -rf`、`curl | sh` 这类高危操作，确认只是"事后放行"。
2. **无路径边界**：文件工具可以读写项目目录之外的任意路径（家目录、系统目录）。
3. **无规则可配**：允许/拒绝行为无法用规则声明，只能靠每次确认。
4. **无全局档位**：用户无法整体切换"严格/宽松"。

## 目标

建立一套**纵深防御的安全检查层**，在工具执行前完成裁决：

- **黑名单**：内置四类高危操作硬拦截（破坏性文件/磁盘操作、远程脚本下载即执行、系统级危险命令、目录破坏），不弹询问。
- **路径沙箱**：文件读写搜索类工具只能落在允许目录（项目根）内，防 `../` 逃逸和符号链接绕过；bash 尽力识别越界。
- **规则引擎**：按"工具 + 参数/路径模式"声明 allow / deny / ask 规则，三级来源（会话级临时 > 项目级 > 用户全局）。
- **权限档位**：严格 / 默认 / 放行三档整体切换，覆盖在具体规则之上，启动默认"默认档"。
- **人在回路（HITL）**：规则未命中时把决定交回用户，支持"本次允许 / 本会话允许 / 永久允许 / 拒绝"，永久允许写入项目级规则。
- **完全接管**：删除旧 `needs_confirmation` 机制，安全检查成为执行前唯一入口。

## 功能需求

- **F1 黑名单拦截** — 内置四类默认黑名单：① 破坏性文件/磁盘操作（`rm -rf`、`del` 递归、`dd`、`mkfs`、`fdisk`）；② 远程脚本下载即执行（`curl|sh`、`wget|bash`、下载后执行）；③ 系统级危险（`shutdown`、`reboot`、`kill` 主进程）；④ 目录破坏（`rm -rf /`、`chmod -R 777 /`）。命中黑名单的操作在工具执行前**硬拦截，不弹 HITL 询问**，并提示原因。
- **F2 路径沙箱** — read_file / write_file / edit_file / glob / grep 的路径参数解析为真实路径（展开 `..`、解析符号链接）后，必须落在允许根（默认项目根 = 启动时工作目录）内，越界则拒绝执行；bash 命令尝试识别明显越界路径（绝对路径指向允许根外、`..` 逃逸）并拦截，识别不了的交给黑名单/规则/询问兜底。
- **F3 规则引擎** — 规则由「工具名 + 模式 + 动作」组成：文件工具的模式用 glob 匹配路径，bash 用正则匹配命令；动作分为 `allow` / `deny` / `ask`。规则从三级来源加载：用户全局、项目级、会话级。
- **F4 权限档位** — 三档整体切换，覆盖在具体规则之上：**严格档**（未命中明确 allow 规则的工具调用一律拦截或询问）、**默认档**（只读操作自动放行、写入/执行类未命中规则时询问、黑名单硬拦，启动默认此档）、**放行档**（只拦截明确 deny / 黑名单，其余直接放行）。通过 `/mode` 命令切换。
- **F5 人在回路（HITL）** — 裁决结果为"询问"时，在终端显示工具名、参数、当前档位和按键菜单：`[a]本次允许`、`[s]本会话允许`、`[n]拒绝`、`[p]永久允许`；回车默认选"本次允许"。选 `s` 产生会话级临时规则（内存），选 `p` 把规则写入项目级 rules.yaml。
- **F6 规则优先级** — 同一操作命中多条规则时按「会话级 > 项目级 > 用户全局」裁决，高优先级规则的结论覆盖低优先级；同层级按配置顺序后者覆盖前者。
- **F7 命令面** — 新增 `/mode strict|default|permissive` 切换档位（切换立即生效）；`/rules` 显示当前档位和三级规则的当前生效集合。
- **F8 完全接管** — 安全层裁决作为工具执行前的唯一入口；删除工具系统中的 `needs_confirmation` 字段及 session 中对应的旧确认分支。

## 非功能需求

- **N1 执行前拦截** — 安全检查在工具真正执行之前完成并生效，被拦截的操作不会产生任何副作用（不写文件、不执行命令）。
- **N2 可测试性** — 策略层（规则加载、裁决、沙箱）与 TUI 和模型解耦，不依赖真实 API，可独立单元测试。
- **N3 不打扰正常开发** — 默认档下只读操作（read_file/glob/grep 且路径在沙箱内）不产生任何确认，开发者体验不被打断。
- **N4 配置一致性** — 规则文件使用 YAML 格式，与现有 config.yaml 一致；加载失败时明确报错且不静默放行。

## 不做的事

- **会话级规则的磁盘持久化** — "本会话允许"只存在内存，会话结束即消失。
- **bash 完整静态分析** — 不做命令的语义解析/沙箱执行（chroot/seccomp/容器隔离），仅靠黑名单 + 正则规则 + 尽力路径检查 + 询问兜底。
- **网络/端口级安全** — 不限制工具访问外网，不扫描端口。
- **规则热重载** — 修改规则文件后需重启或 `/mode` 重新触发加载，不做文件监听。
- **多用户/多角色权限** — 规则面向单个本地用户，不做角色体系。
- **对 model API 请求本身的鉴权** — 只保护本机工具执行。

## 验收标准

| 编号 | 验收条件 | 对应需求 |
|------|---------|---------|
| AC1 | 黑名单命令（如 `rm -rf /tmp/x`、`curl http://x | sh`）被硬拦截，且不出现 HITL 询问，提示原因（验证：调策略层裁决观察结果） | F1 |
| AC2 | 越界路径被拒绝：`../` 逃逸、符号链接指向项目外、绝对路径指向 `/etc` 等（验证：调沙箱校验） | F2 |
| AC3 | 沙箱内路径正常放行（验证：合法路径通过校验） | F2 |
| AC4 | 三级规则优先级正确：同一工具同参数，会话级 deny 覆盖项目级 allow（验证：构造三级规则测试） | F3, F6 |
| AC5 | `/mode strict` 后未命中 allow 的操作被拦截/询问；`/mode permissive` 后仅黑名单/deny 被拦（验证：切换档位观察裁决） | F4 |
| AC6 | HITL 选 `a` 本次执行、`s` 会话内后续同类放行、`n` 拒绝、`p` 写入项目规则（验证：交互实测） | F5 |
| AC7 | `p` 选永久允许后，项目级 rules.yaml 出现对应规则，重启会话后仍生效（验证：读文件 + 重启实测） | F5 |
| AC8 | 工具系统不再有 needs_confirmation，session 无旧确认分支（验证：grep 确认无残留） | F8 |
| AC9 | 策略层单元测试全部通过，默认档下只读操作零确认（验证：pytest） | N2, N3 |
