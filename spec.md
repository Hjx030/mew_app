# MewCode v0.3 — Agent Loop Spec

## 背景

MewCode v0.2 完成了单轮工具调用：AI 请求工具 → 执行 → 回送 → AI 回复即停。
v0.3 将其升级为自动循环（Agent Loop）：AI 可以连续多次调用工具，链式完成复杂任务，
直到主动给出最终回复。

## 目标

- Agent Loop：模型执行完工具后可继续调用更多工具，不再一轮止步
- 循环上限：最大 10 步工具调用，防止失控
- 确认策略：首步需确认的工具在用户确认后，本轮循环后续自动放行
- TUI 清晰展示多步工具调用的链条
- 支持 Ctrl+C 随时中断正在执行的循环

## 功能需求

- **F1: Agent Loop 循环** — 执行完工具并将结果回送后，再次以同样带 tools 的请求调用 API，
  让模型可继续调用工具或给出文字回复
- **F2: 循环终止条件** — 当模型返回文字回复（不触发 tool_call）时终止循环；
  模型返回 tool_call 则继续
- **F3: 最大步数限制** — 单次对话的 Agent Loop 最多执行 10 步工具调用，
  超出后强制结束，输出已达上限提示
- **F4: 自动确认** — 同一轮 Agent Loop 中，needs_confirmation=true 的工具
  首步需用户确认，后续循环自动放行
- **F5: Ctrl+C 中断** — 用户可随时中断正在执行的循环，已执行的结果保留在对话历史中
- **F6: TUI 步进展示** — 循环中每一步都显示工具名、参数、结果，以及当前步数（如 2/10）
- **F7: 错误容忍** — 单步工具执行失败不终止循环，错误信息回送模型让模型决策下一步

## 非功能需求

- **N1: 循环叠加成本控制** — 每步都产生 API 调用，TUI 应显示当前已消耗的步数
- **N2: 响应性** — 循环期间终端保持可中断（Ctrl+C）

## 不做的事

- **并行工具调用** — 一次仍只处理一个 tool_call
- **工具调用超时后重试** — 超时即报错，不自动重试
- **循环间持久化状态** — 每次循环独立 API 请求，不在工具间共享内存状态
- **人机协作审批** — 每步都弹确认不在本版本范围

## 验收标准

| 编号 | 验收条件 | 对应需求 |
|------|---------|---------|
| AC1 | 输入"创建 hello.py，打印 Hello World，然后读取它" → 模型依次调用 write_file → read_file → 文字回复 | F1, F2 |
| AC2 | 输入复杂指令触发超过 10 步 → 第 10 步后强制终止，显示提示 | F3 |
| AC3 | 首次执行 bash 时弹确认，确认后同一轮循环中再次调用 bash 自动放行 | F4 |
| AC4 | Agent Loop 执行中按 Ctrl+C → 循环终止，回到输入状态 | F5 |
| AC5 | TUI 中显示 "步骤 2/10" 以及完整的工具调用链 | F6 |
| AC6 | 中间某步工具失败 → 错误回送模型，模型继续后续步骤而非崩溃 | F7 |
