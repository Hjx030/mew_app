# MewCode Prompt 系统重构 Checklist (v0.4)

> 每一项通过运行代码或观察行为来验证，聚焦系统行为。

## 实现完整性

- [ ] **[全局指令模块化]** 拼装入口输出的全局指令包含 5 个模块（identity/behavior/tool_usage/safety/output_style）的内容（验证：`python -c` 调用 build_stable，输出含各模块关键词）
- [ ] **[环境块注入]** 对话首条 user 消息开头包含工作目录、操作系统、当前时间三字段，且时间随轮次刷新（验证：观察 session 组装的 user 消息内容）
- [ ] **[稳定前缀一致]** 相邻两轮中，稳定指令 + 环境块之前的消息序列字节一致（验证：构造两轮请求消息对比前缀）
- [ ] **[Plan 变频注入]** `/plan on` 后第 1 轮注入完整指令、第 2–3 轮精简、第 4 轮完整（验证：PlanModeInjector 单元测试 + 会话观察）
- [ ] **[温和提醒]** Agent Loop 中工具调用达 5 次时注入一次温和提醒（验证：单测覆盖 + 观察注入消息）
- [ ] **[双重强化]** 优先专用工具 / bash 谨慎 / 编辑前先读 三条规则同时出现在全局指令和对应工具描述中（验证：grep 工具 description + 拼装输出对比）
- [ ] **[usage 解析]** Provider 从流式响应中解析出 `usage` 事件，含 hit/miss 字段（验证：test_providers 新增单测断言）
- [ ] **[缓存展示]** TUI 显示缓存命中/未命中 token 数（验证：真实请求观察 render_cache_hit 输出）

## 集成

- [ ] **session 接入 prompt 包** 会话消息序列以"稳定指令 + 环境块"开头（验证：启动会话观察首条消息）
- [ ] **现有命令兼容** `/plan` `/tools` `/clear` `/exit` 行为与 v0.3 一致（验证：逐命令手动操作）

## 编译与测试

- [ ] 全部测试通过（验证：`python -m pytest -q`，期望 26 + 新增全绿）
- [ ] 无导入错误、版本号正常（验证：`python -m mewcode --version` 输出 0.3.0）

## 端到端场景

- [ ] **场景 1（真实缓存命中）**：连续两轮真实对话请求，第 2 轮起 `prompt_cache_hit_tokens > 0`（验证：TUI 显示命中信息；DeepSeek 缓存 best-effort，若环境未命中需连续多轮观察）
- [ ] **场景 2（Plan 变频）**：`/plan on` 后连续 4 轮对话，观察注入内容为 完整→精简→精简→完整（验证：会话日志/输出）
- [ ] **场景 3（温和提醒）**：触发一次 5 步工具链式任务，第 5 次工具调用后模型收到收敛提醒（验证：观察会话输出）
- [ ] **场景 4（环境感知）**：对话首条消息含当前工作目录路径，模型能据此定位文件（验证：首条消息输出观察）
