# MewCode Prompt 系统重构 Spec (v0.4)

## 背景

MewCode v0.3 已实现 Agent Loop + Plan Mode。当前的 system prompt 架构存在四个问题：

1. **全局指令是单条固定字符串**（`BASE_SYSTEM_PROMPT + PLAN_MODE_INSTRUCTION` 拼接），身份、行为、工具使用、安全等职责混在一起，无法按优先级调整、难以插入新模块。
2. **环境信息缺失**：模型不知道当前工作目录、操作系统、时间，只能靠猜或从用户输入推断。
3. **Plan Mode 指令写死在首条 system**：每次 `/plan` 切换或 `/clear` 都会重建整条 system prompt，且开关一旦变动就影响前缀稳定性，DeepSeek 的自动前缀缓存难以生效。
4. **缓存不可观测**：Provider 不解析 API 返回的缓存命中字段，无法确认缓存策略是否真的生效。

## 目标

- 建立**模块化、可扩展**的全局指令拼装机制：按职责拆分模块，按优先级排序，可插入新模块。
- 区分**稳定内容**（全局指令、工具描述 → 可缓存前缀）与**变化内容**（环境信息、对话历史、动态注入 → 对话通道）。
- 环境信息作为独立的**半稳定块**注入，放在稳定指令之后、对话之前，避免环境变化拖垮整个缓存前缀。
- 用**特殊标签消息通道**在运行时注入温和提示，不污染缓存、不被模型当作用户输入回复。
- Plan Mode 指令从全局指令中拆出，**按轮次变频注入**（首轮完整 / 每 3 轮完整 / 其余精简）。
- 在工具描述和全局指令中**双重强化**三条关键规则：优先专用工具、bash 谨慎执行、编辑前先读。
- 解析 DeepSeek API 返回的**缓存命中字段**，用真实请求验证缓存策略确实生效。

## 功能需求

- **F1 模块化拼装** — 全局指令按职责拆成多个模块（身份、行为、工具使用、安全边界、输出风格），每个模块带优先级，按优先级排序拼装成完整全局指令；新增模块可插入到排序链中。
- **F2 稳定/变化分流** — 对话前缀由稳定内容构成：模块化全局指令 + 工具描述；环境信息、对话历史、动态注入内容走变化通道，不进入稳定前缀。
- **F3 环境信息注入** — 工作目录、操作系统、当前时间作为环境块注入到对话中，位置在稳定指令之后、用户对话之前；每次对话开始时实时刷新这三个字段。
- **F4 双重强化** — 三条规则（优先专用工具而非 bash、bash 执行谨慎、write/edit 前必须先 read）同时出现在全局指令中**和**对应工具的描述中。
- **F5 特殊标签注入通道** — 支持以带标记的消息形式在运行时向模型注入补充指令；该消息不被当作需要回复的用户问题；注入动作不改变稳定前缀内容（不破坏缓存）。
- **F6 Plan Mode 变频注入** — 保留 `/plan on|off` 手动开关。开启后：启用后的第 1 轮注入完整 Plan 指令，每满 3 轮重复注入一次完整指令，其余轮次注入一行精简提醒；关闭后不注入。
- **F7 温和提示** — 当单轮 Agent Loop 中工具调用次数达到 5 次时，通过注入通道向模型发送"注意收敛"类提醒；提醒只在触发当轮注入一次。
- **F8 缓存命中观测** — Provider 解析 DeepSeek API 返回中的缓存命中字段（prompt_cache_hit_tokens / prompt_cache_miss_tokens），并将命中信息暴露给会话层。

## 非功能需求

- **N1 缓存友好** — 稳定前缀在相邻轮次之间保持字节级一致；环境信息的变化不影响已稳定前缀的缓存命中。
- **N2 可测试性** — 拼装逻辑与 TUI 解耦，可独立进行单元测试（不依赖真实 API）。
- **N3 向后兼容** — 现有交互命令 `/plan`、`/tools`、`/clear`、`/exit` 的行为保持不变。
- **N4 成本可控** — 变频注入机制避免每轮重复注入完整 Plan 指令，控制 token 消耗。

## 不做的事

- **Anthropic cache_control 显式断点** — 本轮只针对 DeepSeek 自动前缀缓存设计。
- **"新工具上线"注入** — 特殊标签通道的该用例本轮不实现。
- **"模式提醒"注入** — 特殊标签通道的该用例本轮不实现（温和提示是唯一用例）。
- **Git 状态字段** — 环境块本轮只含工作目录、操作系统、当前时间。
- **对话记忆/持久化** — 与 prompt 重构无关，留给后续版本。
- **跨会话缓存策略** — 本轮只关注单会话内的轮次间缓存。

## 验收标准

| 编号 | 验收条件 | 对应需求 |
|------|---------|---------|
| AC1 | 全局指令由多个模块拼装，且新增一个模块后自动纳入拼装结果（验证：调用拼装入口，观察输出含所有模块内容） | F1 |
| AC2 | 对话首条消息包含环境块，且工作目录、操作系统、当前时间三个字段均存在（验证：输出观察） | F3 |
| AC3 | 相邻两轮对话中，稳定前缀（全局指令 + 工具描述）字节级一致，环境块变化不影响它（验证：对比两轮消息序列） | F2, N1 |
| AC4 | read_file / write_file / edit_file / bash 的 Schema 描述中包含对应强化规则，且全局指令中也包含这三条规则（验证：grep 工具描述 + 拼装输出） | F4 |
| AC5 | 工具调用达阈值时，模型收到带标记的注入消息，且该消息被模型当作指令而非问题来回复（验证：观察触发轮模型行为） | F5, F7 |
| AC6 | `/plan on` 后：第 1 轮含完整 Plan 指令，第 2–3 轮只有精简提醒，第 4 轮重复完整指令（验证：观察各轮注入的消息内容） | F6 |
| AC7 | 使用真实 API 请求连续对话，从第 2 轮起解析到缓存命中字段且命中 token 数 > 0（验证：TUI/日志显示命中数据） | F8 |
| AC8 | 现有命令 `/plan`、`/tools`、`/clear`、`/exit` 行为与 v0.3 一致（验证：逐命令手动操作） | N3 |
